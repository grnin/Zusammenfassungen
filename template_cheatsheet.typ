#import "./template_zusammenf.typ": *

#import "./template--project.typ": *


//
#set heading(
    numbering: none,
    hanging-indent: 30pt,
    // inset: (left: 20cm)
    // offset: -100,
)
// #set heading(numbering: (first, ..other) => if other.pos().len() == 2 { return first })
// #show heading: it => block(it.body)
// #show heading.where(level)

//
